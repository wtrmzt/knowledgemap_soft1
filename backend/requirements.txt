flask==3.1.0
flask-cors==5.0.1
flask-sqlalchemy==3.1.1
sqlalchemy==2.0.40
pyjwt==2.10.1
python-dotenv==1.1.0
openai==1.75.0
numpy==2.0.0
pandas==2.2.3
requests==2.32.3
gunicorn==23.0.0
psycopg2-binary==2.9.10
spacy>=3.7.0
# ===== Phase 1: Redis / レート制限 =====
redis>=5.0.0,<6.0.0

# ===== Phase 1: WSGI 性能向上 =====
gevent>=23.9.0
gunicorn>=21.2.0  # 既存にあれば不要

# ===== Phase 1: マイグレーション =====
alembic>=1.13.0

# ===== Phase 1: 非同期ジョブ (Celery を採用する場合のみ) =====
celery>=5.3.0,<6.0.0

# ===== Phase 2: Google ログイン =====
google-auth>=2.27.0,<3.0.0

# ===== Phase 4: エラー監視 (推奨) =====
sentry-sdk[flask]>=1.40.0

# ===== Phase 3: S3 を使う場合 =====
# boto3>=1.34.0

# ===== v3 で追加する依存ライブラリ =====
# 既存の requirements.txt の末尾に追記してください.

# Redis(任意。未設定なら in-memory フォールバック)
redis>=5.0.0,<6.0.0

# Google ログイン(任意。GOOGLE_CLIENT_ID 未設定なら使われない)
google-auth>=2.27.0,<3.0.0

# WSGI 性能向上(100名同時接続用)
gevent>=23.9.0
# gunicorn は既存にあるはず. 無ければ追加:
# gunicorn>=21.2.0

# (推奨) エラー監視 — 本番投入前に
# sentry-sdk[flask]>=1.40.0

# =====================================================================
#  環境変数の追加分
# =====================================================================

# ---------------------------------------------------------------------
# backend/.env （Flask バックエンド）に追加
# ---------------------------------------------------------------------
